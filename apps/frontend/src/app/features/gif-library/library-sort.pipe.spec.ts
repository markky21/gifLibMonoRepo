import { LibrarySortPipe } from './library-sort.pipe';

describe('LibrarySortPipe', () => {
  it('create an instance', () => {
    const pipe = new LibrarySortPipe();
    expect(pipe).toBeTruthy();
  });
});
